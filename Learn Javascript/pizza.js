var makePizza = function(topping, crustType) {
  return 'The pizza topped with ' + topping + ' and ' + crustType + ' crust is done. Let\'s eat!';
};

var customPizza = makePizza('chesee', 'yes'); 
console.log(customPizza);